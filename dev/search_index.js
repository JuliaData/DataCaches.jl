var documenterSearchIndex = {"docs":
[{"category":"section","location":"integration/#Library-Integration-Guide","page":"Library Integration","text":"DataCaches.jl is designed to be embedded in other packages, not just used interactively. This guide walks through the three standard integration patterns, from the simplest (private internal cache) to the most seamless (fully transparent autocaching with a package-owned default store).\n\nThe patterns are independently composable: a package can use any combination of A, B, and C, or just one. Each pattern is a self-contained module example that compiles and runs.\n\n","title":"Library Integration Guide"},{"category":"section","location":"integration/#Pattern-A-—-Package-private-data-store","page":"Library Integration","text":"When to use: Your package needs to cache data internally for its own operation — precomputed indices, downloaded reference snapshots, expensive one-time computations. Users never interact with the cache directly; from their perspective the data is simply \"already there\".\n\nMechanism: scratch_datacache! creates a DataCache namespaced by your package's UUID, stored in the DataCaches depot. It is isolated from every other package's data and is removed if DataCaches.jl is ever uninstalled and Pkg.gc() is run.\n\nmodule TaxonomyDB\nusing DataCaches\nusing DataFrames\n\n# Copy your UUID from Project.toml\nconst _PKG_UUID = Base.UUID(\"11111111-2222-3333-4444-555555555555\")\n\n# Lazily-initialised cache ref — populated in __init__\nconst _cache_ref = Ref{Union{DataCache,Nothing}}(nothing)\n\nfunction __init__()\n    # One scratch_datacache! per logical dataset; use distinct keys for multiple stores.\n    _cache_ref[] = DataCaches.scratch_datacache!(_PKG_UUID, :taxonomy)\nend\n\n# Expose the cache for power users who want to inspect or manage it\ncache() = _cache_ref[]\n\n# --- Public API ---\n\n\"\"\"\n    TaxonomyDB.get_taxa(group) -> DataFrame\n\nReturn the taxon list for `group`, downloading and caching it on first access.\nSubsequent calls within the same or future sessions are instant.\n\"\"\"\nfunction get_taxa(group::String)\n    dc = _cache_ref[]\n    haskey(dc, group) && return dc[group]\n    result = _download_taxa(group)\n    dc[group] = result\n    return result\nend\n\n\"\"\"\n    TaxonomyDB.refresh!(group)\n\nForce a fresh download of `group`, overwriting the cached copy.\n\"\"\"\nfunction refresh!(group::String)\n    dc = _cache_ref[]\n    result = _download_taxa(group)\n    dc[group] = result\n    return result\nend\n\n# --- Internal ---\n\nfunction _download_taxa(group::String)\n    # ... expensive HTTP request, parsing, etc. ...\n    return DataFrame(name = [\"$(group)_sp1\", \"$(group)_sp2\"], rank = [\"species\", \"species\"])\nend\n\nend # module\n\nUser-facing experience — the cache is invisible:\n\nusing TaxonomyDB\n\n# First call: downloads and caches transparently\ndf = TaxonomyDB.get_taxa(\"Canidae\")\n\n# Subsequent calls (same session or future sessions): instant\ndf = TaxonomyDB.get_taxa(\"Canidae\")\n\n# Power users can inspect or manage the store if they want to\nshowcache(TaxonomyDB.cache())\nclear!(TaxonomyDB.cache())\n\n","title":"Pattern A — Package-private data store"},{"category":"section","location":"integration/#Pattern-B-—-Instrumented-functions,-user-controls-cache","page":"Library Integration","text":"When to use: You want users to be able to call set_autocaching!(true) and have your functions benefit automatically, but you are happy for caching to go to wherever the user points — the DataCaches shared default or a cache they explicitly choose. Your package does not own any cache storage.\n\nMechanism: Wrap each public function with autocache. When autocaching is off, autocache calls the real function directly with zero overhead. When on, it checks the user-configured store and returns cached results or fetches and stores fresh ones.\n\nmodule WeatherAPI\nusing DataCaches\nusing DataFrames\nimport DataCaches: autocache\n\n# --- Public API ---\n\n\"\"\"\n    WeatherAPI.forecast(city; days=7) -> DataFrame\n\nFetch a weather forecast for `city` over the next `days` days.\nSupports DataCaches autocaching — call `set_autocaching!(true)` before use.\n\"\"\"\nfunction forecast(city::String; days::Int = 7)\n    return autocache(\n        () -> _fetch_forecast(city; days = days),   # the real work, as a zero-arg closure\n        forecast,                                    # this function (used for autocache allowlist)\n        \"weather/forecast\",                          # stable endpoint string (part of cache key)\n        (; city = city, days = days),                # kwargs (part of cache key)\n    )\nend\n\n\"\"\"\n    WeatherAPI.current(city) -> DataFrame\n\"\"\"\nfunction current(city::String)\n    return autocache(\n        () -> _fetch_current(city),\n        current,\n        \"weather/current\",\n        (; city = city),\n    )\nend\n\n# --- Internal ---\n\nfunction _fetch_forecast(city; days)\n    # ... real HTTP request ...\n    return DataFrame(day = 1:days, city = fill(city, days), temp_c = rand(days) .* 20 .+ 10)\nend\n\nfunction _fetch_current(city)\n    return DataFrame(city = [city], temp_c = [rand() * 20 + 10])\nend\n\nend # module\n\nUser-facing experience:\n\nusing WeatherAPI, DataCaches\n\n# --- No caching (default) ---\ndf = WeatherAPI.forecast(\"London\"; days = 5)   # always live\n\n# --- Enable autocaching: use DataCaches shared default ---\nset_autocaching!(true)\ndf = WeatherAPI.forecast(\"London\"; days = 5)   # fetches + stores\ndf = WeatherAPI.forecast(\"London\"; days = 5)   # instant, from cache\nset_autocaching!(false)\n\n# --- Use a specific store for this project ---\nmy_cache = DataCache(:weather_project)\nset_autocaching!(true; cache = my_cache)\ndf = WeatherAPI.forecast(\"Paris\"; days = 3)    # fetches + stores in my_cache\nset_autocaching!(false)\n\n# --- Cache only specific functions ---\nset_autocaching!(true, WeatherAPI.forecast)    # only forecast; current is always live\ndf  = WeatherAPI.forecast(\"Tokyo\")             # cached\ncur = WeatherAPI.current(\"Tokyo\")              # not cached\nset_autocaching!(false)\n\n# --- Force refresh ---\nset_autocaching!(true)\ndf = WeatherAPI.forecast(\"London\"; days = 5; force_refresh = true)  # re-fetches even if cached\nset_autocaching!(false)\n\n","title":"Pattern B — Instrumented functions, user controls cache"},{"category":"section","location":"integration/#Pattern-C-—-Instrumented-functions-with-package-owned-default-cache","page":"Library Integration","text":"When to use: You want the benefits of Pattern B (user-visible set_autocaching! control) plus namespace isolation: when the user enables autocaching without specifying a store, results should go to your package's own scratch store — not the shared DataCaches default. The user can still override at any time by passing an explicit cache to set_autocaching!.\n\nMechanism: Create a scratch_datacache! in __init__ and pass it as package_cache to every autocache call. The store-resolution priority is:\n\nUser-explicit: set_autocaching!(true; cache=x) → always uses x\npackage_cache → used when no explicit user cache was set\ndefault_filecache() → final fallback\n\nmodule BioSearch\nusing DataCaches\nusing DataFrames\nimport DataCaches: autocache\n\n# Copy your UUID from Project.toml\nconst _PKG_UUID = Base.UUID(\"aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee\")\n\nconst _pkg_cache_ref = Ref{Union{DataCache,Nothing}}(nothing)\n\nfunction __init__()\n    _pkg_cache_ref[] = DataCaches.scratch_datacache!(_PKG_UUID, :biosearch)\nend\n\n\"\"\"\n    BioSearch.pkg_cache() -> DataCache\n\nReturn BioSearch's own package-scoped cache.\nUsed as the default store for autocaching unless the user supplies an explicit one.\n\"\"\"\npkg_cache() = _pkg_cache_ref[]\n\n# --- Public API ---\n\n\"\"\"\n    BioSearch.search_species(; kwargs...) -> DataFrame\n\nSearch the biodiversity database. Supports DataCaches autocaching.\nWhen autocaching is enabled without an explicit cache, results are stored in\nBioSearch's own package-scoped store (see [`BioSearch.pkg_cache`](@ref)).\n\"\"\"\nfunction search_species(; kwargs...)\n    return autocache(\n        () -> _do_search_species(; kwargs...),\n        search_species,\n        \"species/search\",\n        kwargs;\n        package_cache = _pkg_cache_ref[],   # ← the key addition vs Pattern B\n    )\nend\n\n\"\"\"\n    BioSearch.occurrences(taxon; kwargs...) -> DataFrame\n\"\"\"\nfunction occurrences(taxon::String; kwargs...)\n    return autocache(\n        () -> _do_occurrences(taxon; kwargs...),\n        occurrences,\n        \"occurrences/list\",\n        merge((; taxon = taxon), kwargs);\n        package_cache = _pkg_cache_ref[],\n    )\nend\n\n# --- Internal ---\n\nfunction _do_search_species(; kwargs...)\n    # ... real HTTP request ...\n    return DataFrame(name = [\"Species A\", \"Species B\"], rank = [\"species\", \"species\"])\nend\n\nfunction _do_occurrences(taxon; kwargs...)\n    return DataFrame(taxon = fill(taxon, 3), lat = rand(3), lon = rand(3))\nend\n\nend # module\n\nUser-facing experience:\n\nusing BioSearch, DataCaches\n\n# --- No caching (default) ---\ndf = BioSearch.search_species(taxon = \"Canidae\")   # always live\n\n# --- Enable autocaching without explicit cache ---\n# Results go to BioSearch's own package-scoped store automatically\nset_autocaching!(true)\ndf = BioSearch.search_species(taxon = \"Canidae\")   # fetches + stores in BioSearch's cache\ndf = BioSearch.search_species(taxon = \"Canidae\")   # instant, from BioSearch's cache\nset_autocaching!(false)\n\n# --- User explicitly overrides: their cache wins ---\nmy_cache = DataCache(:my_project)\nset_autocaching!(true; cache = my_cache)\ndf = BioSearch.search_species(taxon = \"Felidae\")   # stored in my_cache, not BioSearch's\nset_autocaching!(false)\n\n# --- Inspect BioSearch's own cache ---\nshowcache(BioSearch.pkg_cache())\n# DataCache: ~/.julia/scratchspaces/<DataCaches-UUID>/caches/module/aaaa.../biosearch (2 entries)\n#   [1]  2026-04-03T10:12:00  3f2a1b9c  search_species(species/search; taxon = \"Canidae\")\n#   [2]  2026-04-03T10:12:05  8d4e7a2f  search_species(species/search; taxon = \"Felidae\")\n\n# --- Clear only BioSearch's cache (does not affect user caches) ---\nclear!(BioSearch.pkg_cache())\n\n","title":"Pattern C — Instrumented functions with package-owned default cache"},{"category":"section","location":"integration/#Storage-formats-and-library-dependencies","page":"Library Integration","text":"","title":"Storage formats and library dependencies"},{"category":"section","location":"integration/#Automatic-format-selection","page":"Library Integration","text":"The storage format for each cache entry is chosen automatically from the data type at write time. No configuration is needed for the common cases:\n\nData type Format File Version-stable?\nDataFrame, Tables.jl-compatible CSV .csv Yes\nNamedTuple JSON .json Yes (JSON-primitive values)\nAnything else Julia serialization .jls No\n\nThe format tag is persisted to the cache index so reads always use the correct deserializer, independent of Julia version.\n\nTo override the automatic selection for a specific entry, pass format= to write!:\n\nwrite!(cache, df; label = \"backup\", format = \"jls\")   # force opaque serialization","title":"Automatic format selection"},{"category":"section","location":"integration/#NamedTuple-JSON-contract","page":"Library Integration","text":"NamedTuple values are stored as JSON. The roundtrip is clean for JSON-primitive types (Int, Float64, String, Bool, arrays of same, nested NamedTuples). Note that Float32 and Float16 values widen to Float64 on read — this is a documented property of the JSON format, not a bug.\n\n","title":"NamedTuple JSON contract"},{"category":"section","location":"integration/#Comparison","page":"Library Integration","text":" Pattern A Pattern B Pattern C\nPackage owns a scratch store Yes No Yes\nFunctions support set_autocaching! No Yes Yes\nDefault store when user omits cache= N/A default_filecache() Package's own store\nUser can override to a different store N/A Yes Yes\nResults isolated from other packages Yes No Yes\nCache removed with package on Pkg.gc() Yes N/A Yes\nUser-visible cache accessor needed Optional No Recommended\n\nPatterns A and C are naturally combined: a package can have both a private internal cache (Pattern A) for reference data that users should never touch, and autocache- instrumented public functions (Pattern C) whose default store users can inspect and manage via the exposed accessor.\n\n","title":"Comparison"},{"category":"section","location":"integration/#Integration-API-reference","page":"Library Integration","text":"","title":"Integration API reference"},{"category":"function","location":"integration/#DataCaches.autocache","page":"Library Integration","text":"autocache(fetch_fn, func, endpoint, kwargs;\n          package_cache::Union{DataCache,Nothing} = nothing,\n          force_refresh::Bool = false)\n\nIntegration hook for library authors: transparently apply autocaching around a fetch closure.\n\nIf autocache is enabled for func, checks the cache for a prior result keyed on (func, endpoint, kwargs). On a hit (and force_refresh = false) returns the cached value immediately. On a miss, calls fetch_fn(), stores the result, and returns it.\n\nIf autocache is not active for func, calls fetch_fn() directly.\n\nArguments\n\nfetch_fn:       Zero-argument callable that performs the real fetch.\nfunc:           The public API function whose autocache opt-in is checked.\nendpoint:       The API endpoint string (e.g. \"occs/list\").\nkwargs:         Keyword arguments passed by the caller.\npackage_cache:  Optional library-owned default DataCache. Used when                   autocache is active but the user did not pass an explicit                   cache to set_autocaching!. Allows a library to default                   to its own scratch_datacache!-backed store while still                   letting the user override via set_autocaching!(true; cache=x).                   Pass nothing (default) to fall through to default_filecache().\nforce_refresh:  When true, bypasses the hit check and overwrites any existing entry.\n\nStore resolution priority\n\nUser-explicit: set_autocaching!(true; cache=x) → always uses x\npackage_cache kwarg → used when no explicit user cache was set\ndefault_filecache() → final fallback\n\n\n\n\n\n","title":"DataCaches.autocache"},{"category":"function","location":"integration/#DataCaches.set_autocaching!","page":"Library Integration","text":"set_autocaching!(enabled::Bool; cache::Union{DataCache,Nothing} = nothing) → Union{DataCache,Nothing}\n\nEnable or disable automatic caching for all instrumented API functions.\n\nWhen enabled=true, every call to an instrumented function automatically stores its result in a DataCache and returns the cached result on subsequent identical calls.\n\nPass cache to use a specific store; that cache is then used regardless of any package_cache the library supplies. When cache is omitted, store resolution is deferred to the autocache call site: any package_cache supplied by the library takes priority, with default_filecache() as the final fallback.\n\nReturns the active DataCache when enabling with an explicit cache, or nothing when disabling (or when enabling without an explicit cache, in which case store selection is deferred).\n\nExamples\n\nset_autocaching!(true)                                  # library default or default_filecache()\nset_autocaching!(false)\nset_autocaching!(true; cache=DataCache(\"/my/project/cache\"))  # explicit store\n\n\n\n\n\nset_autocaching!(enabled::Bool, func; cache::Union{DataCache,Nothing} = nothing) → Union{DataCache,Nothing}\nset_autocaching!(enabled::Bool, funcs::AbstractVector; cache::Union{DataCache,Nothing} = nothing) → Union{DataCache,Nothing}\n\nEnable or disable automatic caching for a specific function (or list of functions).\n\nWhen enabled=true, autocache is activated for func (additive — does not affect other per-function settings). If global autocache is currently on, calling this switches to per-function mode with only {func}.\n\nWhen enabled=false and per-function mode is active, removes func from the allowlist. If the allowlist becomes empty, autocache is fully disabled.\n\nNote: set_autocaching!(false, func) has no effect when global autocache is on; call set_autocaching!(false) to disable globally.\n\nReturns the active DataCache, or nothing when fully disabled.\n\nExamples\n\nDataCaches.set_autocaching!(true, pbdb_occurrences)\nDataCaches.set_autocaching!(true, [pbdb_occurrences, pbdb_taxa])\nDataCaches.set_autocaching!(false, pbdb_occurrences)\n\n\n\n\n\n","title":"DataCaches.set_autocaching!"},{"category":"function","location":"integration/#DataCaches.scratch_datacache!","page":"Library Integration","text":"scratch_datacache!(pkg_uuid::Base.UUID, key::Symbol = :datacache) → DataCache\n\nCreate a DataCache stored under DataCaches' own depot, namespaced by pkg_uuid and key, at <depot>/caches/module/<pkg_uuid>/<key>/.\n\nThe store is managed as part of the DataCaches depot (cleaned up when DataCaches is uninstalled), not the calling package's own lifecycle. Use different key values to create multiple independent stores for the same package UUID.\n\nUse this in a package's __init__ to create a named, module-scoped cache:\n\nmodule MyPackage\nusing DataCaches\n\nconst _MY_UUID = Base.UUID(\"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx\")  # match Project.toml\nconst _CACHE = Ref{Union{DataCache,Nothing}}(nothing)\n\nfunction __init__()\n    _CACHE[] = DataCaches.scratch_datacache!(_MY_UUID, :results)\nend\n\nget_cache() = _CACHE[]\nend\n\n\n\n\n\n","title":"DataCaches.scratch_datacache!"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"constant","location":"api/#DataCaches.SERIALIZER_REGISTRY","page":"API Reference","text":"SERIALIZER_REGISTRY\n\nDict mapping format tags to CacheSerializer instances used for reading. Populated at module load with built-in serializers; extensions add entries via register_serializer!.\n\n\n\n\n\n","title":"DataCaches.SERIALIZER_REGISTRY"},{"category":"type","location":"api/#DataCaches.CacheEntry","page":"API Reference","text":"CacheEntry\n\nA metadata descriptor for a single cached dataset in a DataCache.\n\nA CacheEntry is returned by write!, entries, entry, and related functions. Fields are accessed directly:\n\ne.id            :: String    — unique identifier (UUID)\ne.seq           :: Int       — stable integer index (persisted; use reindexcache! to compact gaps)\ne.label         :: String    — user-assigned label (hash string for @filecache entries; empty if none)\ne.path          :: String    — absolute path to the backing data file\ne.description   :: String    — human-readable source expression (empty if none was recorded)\ne.datecached    :: DateTime  — when the entry was written; typemin(DateTime) if unknown\ne.dateaccessed  :: DateTime  — when the entry was last read; typemin(DateTime) if never accessed\ne.ttl           :: Union{Nothing,Period} — time-to-live; nothing means no expiry\n\nBackward compatibility\n\nCacheKey is a backward-compatible alias for CacheEntry. Existing code using CacheKey continues to work without modification. The alias will gain a deprecation warning in a future release.\n\n\n\n\n\n","title":"DataCaches.CacheEntry"},{"category":"type","location":"api/#DataCaches.CacheSerializer","page":"API Reference","text":"CacheSerializer\n\nAbstract type for cache serialization backends. Implement four methods to define a new serializer:\n\nformat_tag(s)::String — short tag stored in the TOML index (e.g. \"csv\", \"json\")\nfile_extension(s)::String — file extension including dot (e.g. \".csv\")\nwrite_data(s, fpath::String, data) — write data to fpath\nread_data(s, fpath::String) — read and return data from fpath\n\nRegister a new serializer with register_serializer! so it is available for reading entries by format tag.\n\n\n\n\n\n","title":"DataCaches.CacheSerializer"},{"category":"type","location":"api/#DataCaches.DataCache","page":"API Reference","text":"DataCache([store::AbstractString]; track_access=true)\nDataCache(key::Symbol; track_access=true)\n\nA labeled, file-backed key-value store for caching query results across Julia sessions.\n\nData is persisted in store as CSV files (for DataFrame values) or serialized Julia objects (.jls) for anything else. An index file (cache_index.toml) in store keeps track of all entries.\n\nNo argument: the store is placed in the user silo of DataCaches' depot at ~/.julia/scratchspaces/<DataCaches-UUID>/caches/user/_DEFAULT/, automatically cleaned up if DataCaches.jl is uninstalled and Pkg.gc() is run. Set the DATACACHES_DEFAULT_STORE environment variable to override this location. DataCache() is equivalent to DataCache(:_DEFAULT).\n\nSymbol argument (DataCache(:name)): creates a named user store within DataCaches.jl's own depot directory (~/.julia/scratchspaces/<DataCaches-UUID>/caches/user/<name>/). The cache is automatically removed along with DataCaches.jl when the package is uninstalled. This is the recommended approach for users and library authors who want a persistent, named cache without managing filesystem paths or package UUIDs:\n\n# User: a named project cache in DataCaches' scratchspace\ndc = DataCache(:myproject)\n\n# Library author: lifecycle-managed cache in __init__\nconst _CACHE = Ref{Union{DataCache,Nothing}}(nothing)\nfunction __init__()\n    _CACHE[] = DataCache(:mypackage_results)\nend\n\nUse scratch_datacache! instead when you need the cache tied to your own package's lifecycle rather than DataCaches.jl's.\n\ntrack_access keyword: when true (the default), every read updates the dateaccessed field of the corresponding CacheEntry and rewrites the index. This supports LRU inspection via entries and DataCaches.CacheAssets.ls. Set track_access = false to skip this rewrite on caches that are read very frequently or have many entries.\n\nExamples\n\ncache = DataCache()                        # default store: caches/user/_DEFAULT/\ncache = DataCache(:_DEFAULT)                # same as DataCache() — the default store\ncache = DataCache(:myproject)              # named store: caches/user/myproject/\ncache = DataCache(\"/my/project/cache\")     # explicit filesystem path\ncache = DataCache(:logs; track_access = false)  # disable access-time recording\n\n# Write\nkey = write!(cache, df)\nkey = write!(cache, df; label=\"Dinosaur families\")\ncache[\"Trilobites\"] = df          # setindex! sugar\n\n# Read\ndf = read(cache, key)\ndf = read(cache, \"Dinosaur families\")\ndf = cache[\"Dinosaur families\"]\ndf = cache[key]\n\n# Introspect\nentries(cache)    # → Vector{CacheEntry} (primary — filterable, sortable)\nentry(cache, \"Dinosaur families\")  # → CacheEntry (single entry by label)\nlabels(cache)     # → Vector{String} (user-assigned labels only)\nkeys(cache)       # → Vector{CacheEntry} (backward-compat; prefer entries())\nkeylabels(cache)  # → Vector{String} (backward-compat; prefer labels())\nkeypaths(cache)   # → Vector{String}\nlabel(cache, key) # → String\npath(cache, key)  # → String\nhaskey(cache, \"Dinosaur families\")\n\n# Manage\ndelete!(cache, key)\ndelete!(cache, \"Dinosaur families\")\nclear!(cache)\nshowcache(cache)\n\n\n\n\n\n","title":"DataCaches.DataCache"},{"category":"type","location":"api/#DataCaches.PurgePolicy","page":"API Reference","text":"PurgePolicy\n\nConfiguration for automatic cache purging, applied after each write! call when a policy has been set with set_autopurge!.\n\nFields:\n\nmax_age::Union{Nothing,Period} — remove entries older than this age (by datecached)\nmax_idle::Union{Nothing,Period} — remove entries not accessed within this period\nkeep_count::Union{Nothing,Int} — keep only the N most-recently-accessed entries (LRU)\nmax_size_bytes::Union{Nothing,Int} — purge LRU entries until total cache size ≤ limit\nkeep_labeled::Bool — when true, labeled entries are never auto-purged\n\nSee set_autopurge! and DataCaches.CacheAssets.purge!.\n\n\n\n\n\n","title":"DataCaches.PurgePolicy"},{"category":"method","location":"api/#Base.delete!-Tuple{DataCache, CacheEntry}","page":"API Reference","text":"delete!(cache::DataCache, entry::CacheEntry)\ndelete!(cache::DataCache, label::AbstractString)\ndelete!(cache::DataCache, uuid_prefix::AbstractString)\ndelete!(cache::DataCache, n::Integer)\ndelete!(cache::DataCache, specs::AbstractVector)\n\nRemove an entry (or multiple entries) from cache and delete their backing files from disk.\n\nThe AbstractString form first tries to match a label exactly, then falls back to matching the UUID prefix shown in brackets by showcache (e.g. \"2a9d4a87\"). An ambiguous prefix (matching more than one entry) is an error.\n\nThe Integer form identifies the entry by its stable sequence index (as shown in showcache). Use reindexcache! to compact gaps after many deletions.\n\nThe AbstractVector form accepts a vector of any mix of CacheEntry, label String, UUID-prefix String, and sequence index Integer specifiers. All removals are batched into a single index rewrite for efficiency. Unresolvable specifiers are silently skipped.\n\n\n\n\n\n","title":"Base.delete!"},{"category":"method","location":"api/#Base.keys-Tuple{DataCache}","page":"API Reference","text":"keys(cache::DataCache) → Vector{CacheEntry}\n\nReturn all CacheEntry objects stored in cache.\n\nnote: Note\nPrefer entries for new code — it supports filtering and sorting. keys is maintained for backward compatibility.\n\n\n\n\n\n","title":"Base.keys"},{"category":"method","location":"api/#Base.read-Tuple{DataCache, CacheEntry}","page":"API Reference","text":"read(cache::DataCache, entry::CacheEntry) → data\nread(cache::DataCache, label::AbstractString) → data\nread(cache::DataCache, n::Integer) → data\n\nRetrieve a cached dataset by CacheEntry, label string, or stable sequence index. The Integer form uses the sequence index shown in brackets by showcache (e.g. [1], [2]). Use reindexcache! to compact gaps after many deletions.\n\n\n\n\n\n","title":"Base.read"},{"category":"method","location":"api/#DataCaches.autocache-NTuple{4, Any}","page":"API Reference","text":"autocache(fetch_fn, func, endpoint, kwargs;\n          package_cache::Union{DataCache,Nothing} = nothing,\n          force_refresh::Bool = false)\n\nIntegration hook for library authors: transparently apply autocaching around a fetch closure.\n\nIf autocache is enabled for func, checks the cache for a prior result keyed on (func, endpoint, kwargs). On a hit (and force_refresh = false) returns the cached value immediately. On a miss, calls fetch_fn(), stores the result, and returns it.\n\nIf autocache is not active for func, calls fetch_fn() directly.\n\nArguments\n\nfetch_fn:       Zero-argument callable that performs the real fetch.\nfunc:           The public API function whose autocache opt-in is checked.\nendpoint:       The API endpoint string (e.g. \"occs/list\").\nkwargs:         Keyword arguments passed by the caller.\npackage_cache:  Optional library-owned default DataCache. Used when                   autocache is active but the user did not pass an explicit                   cache to set_autocaching!. Allows a library to default                   to its own scratch_datacache!-backed store while still                   letting the user override via set_autocaching!(true; cache=x).                   Pass nothing (default) to fall through to default_filecache().\nforce_refresh:  When true, bypasses the hit check and overwrites any existing entry.\n\nStore resolution priority\n\nUser-explicit: set_autocaching!(true; cache=x) → always uses x\npackage_cache kwarg → used when no explicit user cache was set\ndefault_filecache() → final fallback\n\n\n\n\n\n","title":"DataCaches.autocache"},{"category":"method","location":"api/#DataCaches.clear!-Tuple{DataCache}","page":"API Reference","text":"clear!(cache::DataCache)\n\nRemove all entries from cache and delete their backing files from disk.\n\n\n\n\n\n","title":"DataCaches.clear!"},{"category":"method","location":"api/#DataCaches.default_filecache-Tuple{}","page":"API Reference","text":"default_filecache() → DataCache\n\nReturn the module-level default DataCache used by @filecache. Created lazily on first access using the default Scratch.jl-backed store (see DataCache for details on the default location).\n\n\n\n\n\n","title":"DataCaches.default_filecache"},{"category":"method","location":"api/#DataCaches.entries-Tuple{DataCache}","page":"API Reference","text":"entries(cache::DataCache; kwargs...) → Vector{CacheEntry}\nentries(; kwargs...) → Vector{CacheEntry}\n\nReturn the entries of cache as a Vector{CacheEntry}, optionally filtered and sorted. When called without a cache argument, uses default_filecache().\n\nThis is the primary function for inspecting a cache's contents. Use entry to look up a single entry by label or index, and labels to get just the labels.\n\nFiltering keyword arguments\n\nKeyword Type Effect\npattern AbstractString/Regex Keep entries whose label or description matches\nbefore DateTime Keep entries cached before this time\nafter DateTime Keep entries cached after this time\naccessed_before DateTime Keep entries last accessed before this time\naccessed_after DateTime Keep entries last accessed after this time\nlabeled Bool true = only labeled; false = only unlabeled\nmissing_file Bool true = only entries whose backing file is gone\n\nSorting keyword arguments\n\nKeyword Values\nsortby :seq (default), :label, :date, :date_desc, :dateaccessed,\n :dateaccessed_desc, :size, :size_desc\nrev Bool — reverse the sort order\n\nExamples\n\ndc = DataCache(:myproject)\n\nall_entries   = entries(dc)\nlabeled_only  = entries(dc; labeled = true)\nrecent        = entries(dc; after = DateTime(\"2026-01-01T00:00:00\"))\nlru           = entries(dc; sortby = :dateaccessed_desc)   # oldest-accessed first\nlarge_first   = entries(dc; sortby = :size_desc)\ncanidae       = entries(dc; pattern = r\"canidae\")\nentries()                                                   # default cache\n\nSee also: entry, labels, keys, DataCaches.CacheAssets.ls.\n\n\n\n\n\n","title":"DataCaches.entries"},{"category":"method","location":"api/#DataCaches.entry-Tuple{DataCache, AbstractString}","page":"API Reference","text":"entry(cache::DataCache, label::AbstractString) → CacheEntry\nentry(cache::DataCache, n::Integer) → CacheEntry\nentry(spec) → CacheEntry\n\nLook up a single CacheEntry by label string or sequence index n. When called with a single non-DataCache argument, uses default_filecache().\n\nThrows a KeyError if no matching entry exists.\n\nExamples\n\ndc = DataCache(:myproject)\n\ne = entry(dc, \"dinosaurs\")   # by label\ne = entry(dc, 3)             # by sequence index\ne = entry(\"dinosaurs\")       # default cache, by label\ne = entry(2)                 # default cache, by index\n\nSee also: entries, write!\n\n\n\n\n\n","title":"DataCaches.entry"},{"category":"method","location":"api/#DataCaches.importcache!-Tuple{DataCache, AbstractString}","page":"API Reference","text":"DataCaches.importcache!(dest::DataCache, source; conflict=:overwrite) → DataCache\n\nImport all entries from an external cache source into dest.\n\nsource can be:\n\nA filesystem directory containing a DataCache store (cache_index.toml at its root).\nA .zip file containing a DataCache store at its root.\nAn HTTP/HTTPS URL pointing to a .zip file; downloaded to a temp location first.\n\nThe conflict keyword controls behavior when an entry in source has the same label as an existing entry in dest:\n\n:overwrite (default) — replace the existing entry with the imported one.\n:skip — keep the existing entry, discard the incoming one.\n:error — raise an ErrorException immediately on conflict.\n\nUnlabeled entries are always imported regardless of conflict. Imported entries receive a new UUID, sequence number, and datecached timestamp in dest. Returns dest.\n\n\n\n\n\n","title":"DataCaches.importcache!"},{"category":"method","location":"api/#DataCaches.invalidate!-Tuple{DataCache}","page":"API Reference","text":"invalidate!(cache::DataCache; kwargs...) → DataCache\ninvalidate!(; kwargs...) → DataCache\n\nRemove entries from cache that match the given criteria. All removals are batched into a single index rewrite.\n\nWhen called without a cache argument, uses default_filecache().\n\nFiltering keyword arguments\n\nEntries are first selected by the standard filter set (same as entries):\n\npattern — Regex or String; matched against label, description, or UUID\nfilepath_pattern — Regex or String; matched against full file path\nfilename_pattern — Regex or String; matched against filename only\nformat — String or Regex; matched against the entry's format tag (\"csv\", \"jls\", …)\nbefore::DateTime — entries cached before this time\nafter::DateTime — entries cached after this time\naccessed_before_date::DateTime — entries last accessed before this time\naccessed_after_date::DateTime — entries last accessed after this time\nlabeled::Union{Nothing,Bool} — true = labeled only; false = unlabeled only\n\nInvalidation criteria\n\nstale::Bool = false — further restrict to entries that are past their TTL\npredicate::Union{Nothing,Function} = nothing — further restrict to entries where predicate(entry) returns true\n\nOptions\n\ndry_run::Bool = false — print matching entries to io without deleting\nio::IO = stdout — output stream for dry_run messages\n\nExamples\n\ndc = DataCache(:myproject; default_ttl = Dates.Hour(24))\n\n# Remove all stale entries\ninvalidate!(dc; stale = true)\n\n# Remove entries matching a label pattern\ninvalidate!(dc; pattern = r\"^temp_\")\n\n# Remove CSV entries older than a week\ninvalidate!(dc; format = \"csv\", before = Dates.now() - Dates.Day(7))\n\n# Remove entries matching a custom predicate\ninvalidate!(dc; predicate = e -> startswith(e.description, \"my_func(\"))\n\n# Preview what would be removed\ninvalidate!(dc; stale = true, dry_run = true)\n\nSee also: isstale, DataCaches.CacheAssets.purge!.\n\n\n\n\n\n","title":"DataCaches.invalidate!"},{"category":"method","location":"api/#DataCaches.isstale-Tuple{DataCache, CacheEntry}","page":"API Reference","text":"isstale(cache::DataCache, entry::CacheEntry) → Bool\nisstale(cache::DataCache, label::AbstractString) → Bool\nisstale(entry::CacheEntry) → Bool\nisstale(label::AbstractString) → Bool\n\nReturn true if entry (or the entry identified by label) has exceeded its time-to-live and is considered stale.\n\nThe effective TTL for an entry is its own entry.ttl if set, otherwise the cache's default_ttl. When no TTL applies (both are nothing), returns false.\n\nStaleness is purely informational — read does not enforce it. The caller decides whether to refresh (enabling stale-while-revalidate patterns).\n\nExamples\n\ndc = DataCache(:myproject; default_ttl = Dates.Hour(1))\nwrite!(dc, result; label = \"query1\")\n\nisstale(dc, \"query1\")   # false immediately after write\n# … one hour later …\nisstale(dc, \"query1\")   # true\n\n# Per-entry TTL overrides the cache default:\nwrite!(dc, result2; label = \"short\", ttl = Dates.Minute(5))\nisstale(dc, \"short\")    # true after 5 minutes\n\nSee also: invalidate!, DataCaches.CacheAssets.purge!.\n\n\n\n\n\n","title":"DataCaches.isstale"},{"category":"method","location":"api/#DataCaches.keylabels-Tuple{DataCache}","page":"API Reference","text":"keylabels(cache::DataCache) → Vector{String}\n\nReturn all labels of entries in cache (empty string for unlabeled entries).\n\nnote: Note\nPrefer labels for new code — it returns only user-assigned labels (no empty strings). keylabels is maintained for backward compatibility.\n\n\n\n\n\n","title":"DataCaches.keylabels"},{"category":"method","location":"api/#DataCaches.keypaths-Tuple{DataCache}","page":"API Reference","text":"keypaths(cache::DataCache) → Vector{String}\n\nReturn the file paths of all entries in cache.\n\n\n\n\n\n","title":"DataCaches.keypaths"},{"category":"method","location":"api/#DataCaches.label-Tuple{DataCache, CacheEntry}","page":"API Reference","text":"label(cache::DataCache, entry::CacheEntry) → String\n\nReturn the label associated with entry (same as entry.label).\n\n\n\n\n\n","title":"DataCaches.label"},{"category":"method","location":"api/#DataCaches.labels-Tuple{DataCache}","page":"API Reference","text":"labels(cache::DataCache) → Vector{String}\nlabels() → Vector{String}\n\nReturn all user-assigned labels for entries in cache, excluding unlabeled entries (those keyed only by an auto-generated hash). When called without arguments, uses default_filecache().\n\nExamples\n\ndc = DataCache(:myproject)\ndc[\"foo\"] = 1\ndc[\"bar\"] = 2\n\nlabels(dc)   # → [\"foo\", \"bar\"] (order may vary)\nlabels()     # default cache\n\nSee also: entries, keylabels.\n\n\n\n\n\n","title":"DataCaches.labels"},{"category":"method","location":"api/#DataCaches.memcache_clear!-Tuple{}","page":"API Reference","text":"memcache_clear!()\n\nDiscard all results stored by @memcache for this session.\n\n\n\n\n\n","title":"DataCaches.memcache_clear!"},{"category":"method","location":"api/#DataCaches.migrate_legacy_defaultcache-Tuple{}","page":"API Reference","text":"DataCaches.migrate_legacy_defaultcache(; conflict::Symbol = :skip) → Bool\n\nMigrate the default cache from its pre-Scratch.jl location to the current default store.\n\nBefore DataCaches.jl integrated with Scratch.jl (prior to v0.2.0), the no-argument DataCache() constructor stored data at:\n\n~/.cache/DataCaches/_DEFAULT/\n\nAfter Scratch.jl integration the default moved to the managed scratchspace:\n\n~/.julia/scratchspaces/<DataCaches-UUID>/caches/user/_DEFAULT/\n\n(or the path given by DATACACHES_DEFAULT_STORE if that environment variable is set).\n\nCall this function once after upgrading to v0.2.0 to move any data accumulated under the old location into the new one. It is safe to call multiple times (idempotent).\n\nReturn value:\n\ntrue  — migration was performed; legacy directory existed and was processed.\nfalse — nothing to migrate; legacy directory did not exist (or was already removed).\n\nMigration strategy:\n\nIf the legacy directory does not exist, returns false immediately.\nIf the current default store does not yet exist, the legacy directory is moved wholesale via Base.mv — no re-indexing, no data copying.\nIf the current default store already exists (e.g. DataCache() was already used after upgrading), all entries from the legacy directory are imported via importcache! and then the legacy directory is removed. The conflict keyword controls label collisions between the two stores.\n\nconflict keyword (applies only when both stores already contain data):\n\n:skip (default) — keep the entry already in the current store; discard the legacy one.\n:overwrite — replace the current store's entry with the legacy one.\n:error — raise an ErrorException immediately on any label collision.\n\nExamples\n\n# Migrate once after upgrading:\nDataCaches.migrate_legacy_defaultcache()\n\n# Prefer legacy data when labels collide:\nDataCaches.migrate_legacy_defaultcache(; conflict = :overwrite)\n\n\n\n\n\n","title":"DataCaches.migrate_legacy_defaultcache"},{"category":"method","location":"api/#DataCaches.migrate_v020_defaultcache-Tuple{}","page":"API Reference","text":"DataCaches.migrate_v020_defaultcache(; conflict::Symbol = :skip) → Bool\n\nMigrate the default cache from its v0.2.0 location to the current default store.\n\nIn DataCaches.jl v0.2.0, the no-argument DataCache() constructor stored data at:\n\n<depot>/caches/defaultcache/\n\nIn v0.3.0+ the default moved into the user silo:\n\n<depot>/caches/user/_DEFAULT/\n\n(or the path given by DATACACHES_DEFAULT_STORE if that environment variable is set).\n\nCall this function once after upgrading to move any data accumulated under the old location into the new one. It is safe to call multiple times (idempotent).\n\nReturn value:\n\ntrue  — migration was performed; v0.2.0 directory existed and was processed.\nfalse — nothing to migrate; v0.2.0 directory did not exist (or was already removed).\n\nMigration strategy:\n\nIf the v0.2.0 directory does not exist, returns false immediately.\nIf the new location does not yet exist, the v0.2.0 directory is moved wholesale via Base.mv — no re-indexing, no data copying.\nIf the new location already has data (e.g. DataCache() was already used after upgrading), all entries are imported via importcache! and then the v0.2.0 directory is removed. The conflict keyword controls label collisions.\n\nconflict keyword (applies only when both stores already contain data):\n\n:skip (default) — keep the entry already in the new store; discard the v0.2.0 one.\n:overwrite — replace the new store's entry with the v0.2.0 one.\n:error — raise an ErrorException immediately on any label collision.\n\nExamples\n\n# Migrate once after upgrading:\nDataCaches.migrate_v020_defaultcache()\n\n# Prefer v0.2.0 data when labels collide:\nDataCaches.migrate_v020_defaultcache(; conflict = :overwrite)\n\n\n\n\n\n","title":"DataCaches.migrate_v020_defaultcache"},{"category":"method","location":"api/#DataCaches.movecache!-Tuple{DataCache, AbstractString}","page":"API Reference","text":"DataCaches.movecache!(cache::DataCache, new_path::AbstractString) → DataCache\n\nMove the cache's underlying store directory to new_path, updating cache.store.\n\nBecause cache_index.toml stores paths as relative paths, the TOML file does not need rewriting. In-memory CacheEntry.path fields (which are absolute) are updated in place.\n\nIf new_path does not exist, it is created (including any missing parent directories).\nIf new_path == abspath(cache.store), returns cache unchanged (no-op).\nIf new_path already exists (and differs), throws an error.\n\nThe move is performed with Base.mv, which copies then deletes across filesystem boundaries.\n\n\n\n\n\n","title":"DataCaches.movecache!"},{"category":"method","location":"api/#DataCaches.path-Tuple{DataCache, CacheEntry}","page":"API Reference","text":"path(cache::DataCache, entry::CacheEntry) → String\n\nReturn the file path of the data file backing entry (same as entry.path).\n\n\n\n\n\n","title":"DataCaches.path"},{"category":"method","location":"api/#DataCaches.register_serializer!-Tuple{String, DataCaches.CacheSerializer}","page":"API Reference","text":"register_serializer!(tag::String, s::CacheSerializer)\n\nAdd or replace a serializer in the registry. After registration, cache entries with format == tag will be read using s. Called automatically by package extensions (e.g. FileIOExt for PNG/JPG support).\n\n\n\n\n\n","title":"DataCaches.register_serializer!"},{"category":"method","location":"api/#DataCaches.reindexcache!-Tuple{DataCache}","page":"API Reference","text":"reindexcache!(cache::DataCache)\n\nRenumber all entries 1..n (sorted by current sequence order), closing gaps left by deletions. After reindexcache!, integer indices in showcache output restart from 1 with no gaps.\n\nUse this after many write/delete cycles to keep index numbers manageable.\n\n\n\n\n\n","title":"DataCaches.reindexcache!"},{"category":"method","location":"api/#DataCaches.relabel!-Tuple{DataCache, CacheEntry, AbstractString}","page":"API Reference","text":"relabel!(cache::DataCache, entry::CacheEntry, new_label::AbstractString) → CacheEntry\nrelabel!(cache::DataCache, old_label::AbstractString, new_label::AbstractString) → CacheEntry\nrelabel!(cache::DataCache, n::Integer, new_label::AbstractString) → CacheEntry\n\nRename the label of an existing cache entry without touching its backing data file.\n\nThe CacheEntry overload identifies the entry by its UUID. The AbstractString overload first tries to match old_label as an exact label, then falls back to UUID-prefix matching (same rules as delete!). The Integer overload identifies the entry by its stable sequence index (as shown in showcache).\n\nRaises an error if new_label is already in use by a different entry. Returns the updated CacheEntry.\n\n\n\n\n\n","title":"DataCaches.relabel!"},{"category":"function","location":"api/#DataCaches.scratch_datacache!","page":"API Reference","text":"scratch_datacache!(pkg_uuid::Base.UUID, key::Symbol = :datacache) → DataCache\n\nCreate a DataCache stored under DataCaches' own depot, namespaced by pkg_uuid and key, at <depot>/caches/module/<pkg_uuid>/<key>/.\n\nThe store is managed as part of the DataCaches depot (cleaned up when DataCaches is uninstalled), not the calling package's own lifecycle. Use different key values to create multiple independent stores for the same package UUID.\n\nUse this in a package's __init__ to create a named, module-scoped cache:\n\nmodule MyPackage\nusing DataCaches\n\nconst _MY_UUID = Base.UUID(\"xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx\")  # match Project.toml\nconst _CACHE = Ref{Union{DataCache,Nothing}}(nothing)\n\nfunction __init__()\n    _CACHE[] = DataCaches.scratch_datacache!(_MY_UUID, :results)\nend\n\nget_cache() = _CACHE[]\nend\n\n\n\n\n\n","title":"DataCaches.scratch_datacache!"},{"category":"method","location":"api/#DataCaches.serializer_for-Tuple{Any}","page":"API Reference","text":"serializer_for(data) -> CacheSerializer\n\nInfer the appropriate serializer for data. Add methods to this function to support new types:\n\nDataCaches.serializer_for(data::MyType) = MySerializer()\n\nBuilt-in dispatch priority (most to least specific):\n\nNamedTuple → JSONSerializer\nAbstractDataFrame → CSVSerializer\nAny Tables.jl-compatible type → CSVSerializer\nEverything else → OpaqueSerializer (Julia binary .jls)\n\n\n\n\n\n","title":"DataCaches.serializer_for"},{"category":"method","location":"api/#DataCaches.set_autocaching!-Tuple{Bool, Any}","page":"API Reference","text":"set_autocaching!(enabled::Bool, func; cache::Union{DataCache,Nothing} = nothing) → Union{DataCache,Nothing}\nset_autocaching!(enabled::Bool, funcs::AbstractVector; cache::Union{DataCache,Nothing} = nothing) → Union{DataCache,Nothing}\n\nEnable or disable automatic caching for a specific function (or list of functions).\n\nWhen enabled=true, autocache is activated for func (additive — does not affect other per-function settings). If global autocache is currently on, calling this switches to per-function mode with only {func}.\n\nWhen enabled=false and per-function mode is active, removes func from the allowlist. If the allowlist becomes empty, autocache is fully disabled.\n\nNote: set_autocaching!(false, func) has no effect when global autocache is on; call set_autocaching!(false) to disable globally.\n\nReturns the active DataCache, or nothing when fully disabled.\n\nExamples\n\nDataCaches.set_autocaching!(true, pbdb_occurrences)\nDataCaches.set_autocaching!(true, [pbdb_occurrences, pbdb_taxa])\nDataCaches.set_autocaching!(false, pbdb_occurrences)\n\n\n\n\n\n","title":"DataCaches.set_autocaching!"},{"category":"method","location":"api/#DataCaches.set_autocaching!-Tuple{Bool}","page":"API Reference","text":"set_autocaching!(enabled::Bool; cache::Union{DataCache,Nothing} = nothing) → Union{DataCache,Nothing}\n\nEnable or disable automatic caching for all instrumented API functions.\n\nWhen enabled=true, every call to an instrumented function automatically stores its result in a DataCache and returns the cached result on subsequent identical calls.\n\nPass cache to use a specific store; that cache is then used regardless of any package_cache the library supplies. When cache is omitted, store resolution is deferred to the autocache call site: any package_cache supplied by the library takes priority, with default_filecache() as the final fallback.\n\nReturns the active DataCache when enabling with an explicit cache, or nothing when disabling (or when enabling without an explicit cache, in which case store selection is deferred).\n\nExamples\n\nset_autocaching!(true)                                  # library default or default_filecache()\nset_autocaching!(false)\nset_autocaching!(true; cache=DataCache(\"/my/project/cache\"))  # explicit store\n\n\n\n\n\n","title":"DataCaches.set_autocaching!"},{"category":"method","location":"api/#DataCaches.set_autopurge!-Tuple{DataCache}","page":"API Reference","text":"set_autopurge!(cache::DataCache; enabled=true, kwargs...) → DataCache\nset_autopurge!(; enabled=true, kwargs...) → DataCache\n\nConfigure an automatic purge policy on cache. After each write!, the policy is applied to keep the cache within the specified limits.\n\nWhen called without a cache argument, uses default_filecache().\n\nPass enabled = false to remove the current policy (disabling auto-purge).\n\nKeyword arguments\n\nmax_age::Union{Nothing,Period} — remove entries older than this age (by datecached)\nmax_idle::Union{Nothing,Period} — remove entries not accessed within this period\nkeep_count::Union{Nothing,Int} — keep only the N most-recently-accessed entries\nmax_size_bytes::Union{Nothing,Int} — purge LRU entries until total cache size ≤ limit\nkeep_labeled::Bool = false — when true, labeled entries are never auto-purged\n\nExamples\n\ndc = DataCache(:myproject)\n\n# Keep only the 20 most recently accessed entries\nset_autopurge!(dc; keep_count = 20)\n\n# Remove entries older than 30 days, never delete labeled entries\nset_autopurge!(dc; max_age = Dates.Day(30), keep_labeled = true)\n\n# Disable auto-purge\nset_autopurge!(dc; enabled = false)\n\nSee also: DataCaches.CacheAssets.purge!, invalidate!.\n\n\n\n\n\n","title":"DataCaches.set_autopurge!"},{"category":"method","location":"api/#DataCaches.set_default_filecache!-Tuple{DataCache}","page":"API Reference","text":"set_default_filecache!(cache::DataCache)\n\nReplace the module-level default cache used by @filecache.\n\n\n\n\n\n","title":"DataCaches.set_default_filecache!"},{"category":"method","location":"api/#DataCaches.showcache-Tuple{DataCache}","page":"API Reference","text":"showcache(cache::DataCache)\n\nPrint a detailed summary of all entries in cache. Equivalent to show(stdout, MIME\"text/plain\"(), cache).\n\n\n\n\n\n","title":"DataCaches.showcache"},{"category":"method","location":"api/#DataCaches.write!-Tuple{DataCache, Any}","page":"API Reference","text":"write!(cache::DataCache, data; label::AbstractString = \"\", description::AbstractString = \"\", format::Union{String,Nothing} = nothing) → CacheEntry\n\nStore data in cache and return a CacheEntry describing the stored item.\n\nIf label is given and another entry with that label already exists, it is silently replaced. The storage format is selected automatically based on the data type:\n\nDataFrame and other Tables.jl-compatible types → .csv\nNamedTuple → .json (JSON-primitive values only; Float32/Float16 widen to Float64)\nImages (Matrix{<:Colorant}) → .png (requires FileIO to be loaded)\nAnything else → .jls (Julia binary serialization; not stable across Julia versions)\n\nPass format explicitly (e.g. format = \"jls\") to override the automatic selection.\n\ndescription is an optional human-readable string (e.g. the source expression) stored alongside the entry for display.\n\nThe returned CacheEntry can be passed directly to read, delete!, relabel!, and related functions, or retrieved later by label using entry.\n\n\n\n\n\n","title":"DataCaches.write!"},{"category":"macro","location":"api/#DataCaches.@filecache!-Tuple{Any}","page":"API Reference","text":"@filecache! expr\n@filecache! cache expr\n\nEvaluate expr (a function call), unconditionally store the result in a DataCache, and return the result. Unlike @filecache, this macro always re-executes the function and overwrites any existing cached entry, making it useful for forcing a cache refresh.\n\nThe one-argument form uses default_filecache(). Pass an explicit DataCache as the first argument to target a specific store.\n\nA @debug message is emitted (visible when ENV[\"JULIA_DEBUG\"] = \"DataCaches\") announcing the update.\n\nExamples\n\n# Force-refresh the default cache\noccs = @filecache! pbdb_occurrences(base_name=\"Canidae\", show=\"full\")\n\n# Force-refresh a specific cache\nmy_cache = DataCache(\"/data/pbdb_cache\")\noccs = @filecache! my_cache pbdb_occurrences(base_name=\"Canidae\")\n\n\n\n\n\n","title":"DataCaches.@filecache!"},{"category":"macro","location":"api/#DataCaches.@filecache-Tuple{Any}","page":"API Reference","text":"@filecache expr\n@filecache cache expr\n\nEvaluate expr (a function call) and store the result in a DataCache, persisting it across Julia sessions. Subsequent calls with identical arguments load from cache without executing the function again.\n\nThe one-argument form uses default_filecache(). Pass an explicit DataCache as the first argument to use a different store.\n\nExamples\n\noccs = @filecache pbdb_occurrences(base_name=\"Canidae\", show=\"full\")\n\nmy_cache = DataCache(\"/data/pbdb_cache\")\noccs = @filecache my_cache pbdb_occurrences(base_name=\"Canidae\")\n\n\n\n\n\n","title":"DataCaches.@filecache"},{"category":"macro","location":"api/#DataCaches.@memcache-Tuple{Any}","page":"API Reference","text":"@memcache expr\n\nEvaluate expr (a function call) and cache the result in memory for the current Julia session. Subsequent calls with identical arguments return the cached value without re-executing the function.\n\nThe cache is keyed on the runtime values of all arguments. Use memcache_clear! to discard cached results.\n\nExample\n\noccs = @memcache pbdb_occurrences(base_name=\"Canidae\", show=\"full\")\ntaxa = @memcache pbdb_taxa(name=\"Dinosauria\")\n\n\n\n\n\n","title":"DataCaches.@memcache"},{"category":"section","location":"cache_assets/#Cache-Assets-—-asset-management-within-a-cache","page":"Cache Assets","text":"DataCaches.CacheAssets is a submodule (public, not exported) that provides a filesystem-style interface for managing individual assets (entries) within a DataCache. Import it as using DataCaches.CacheAssets or access functions as DataCaches.CacheAssets.<function>.\n\nAll functions accept an optional leading DataCache argument. When omitted, default_filecache() is used.","title":"Cache Assets — asset management within a cache"},{"category":"section","location":"cache_assets/#List-assets","page":"Cache Assets","text":"ls returns a Vector{CacheEntry} for programmatic use; ls! prints a formatted listing to an IO stream and returns nothing. Both accept the same filtering and sorting keyword arguments.\n\nnote: Note\nentries (exported at the top level) provides the same data-retrieval functionality without importing CacheAssets. Use CacheAssets.ls! when you want formatted output.\n\nusing DataCaches.CacheAssets\n\n# --- Data form: returns Vector{CacheEntry} ---\n\nentries = CacheAssets.ls(dc)\n\n# Filter by label/description pattern\nentries = CacheAssets.ls(dc; pattern = r\"canidae\")\nentries = CacheAssets.ls(dc; pattern = \"dino\")          # string is converted to Regex\n\n# Filter by file path (full path or filename only)\nentries = CacheAssets.ls(dc; filepath_pattern = r\"/project/caches/\")\nentries = CacheAssets.ls(dc; filepath_pattern = \"/scratch\")    # string → Regex\nentries = CacheAssets.ls(dc; filename_pattern = r\"^abc1234\")   # UUID-prefix match\nentries = CacheAssets.ls(dc; filename_pattern = r\"\\.csv$\")     # CSV-backed entries only\n\n# Filter by write or access date\nentries = CacheAssets.ls(dc; after  = DateTime(\"2026-01-01T00:00:00\"))\nentries = CacheAssets.ls(dc; before = DateTime(\"2026-06-01T00:00:00\"))\nentries = CacheAssets.ls(dc; accessed_after_date  = DateTime(\"2026-03-01T00:00:00\"))\nentries = CacheAssets.ls(dc; accessed_before_date = DateTime(\"2026-06-01T00:00:00\"))\n\n# Filter by whether the entry has a label\nentries = CacheAssets.ls(dc; labeled = true)   # only labeled entries\nentries = CacheAssets.ls(dc; labeled = false)  # only unlabeled entries\n\n# Filter by format tag\nentries = CacheAssets.ls(dc; format = \"jls\")        # exact match\nentries = CacheAssets.ls(dc; format = r\"csv|json\")  # Regex match\n\n# Sort\nentries = CacheAssets.ls(dc; sortby = :date_desc)          # newest first\nentries = CacheAssets.ls(dc; sortby = :dateaccessed_desc)  # least recently used last\nentries = CacheAssets.ls(dc; sortby = :size_desc)          # largest first (requires stat)\nentries = CacheAssets.ls(dc; sortby = :label)\n\n# Use default cache (default_filecache())\nentries = CacheAssets.ls()\nentries = CacheAssets.ls(; sortby = :date_desc)\n\n# --- Display form: prints to io, returns nothing ---\n\n# Default: normal detail level, sorted by sequence index\nCacheAssets.ls!(dc)\n\n# Terse — just labels\nCacheAssets.ls!(dc; detail = :minimal)\n\n# Full — includes last-access time, file size, and data format\nCacheAssets.ls!(dc; detail = :full)\n\n# All ls filter/sort kwargs are also accepted by ls!\nCacheAssets.ls!(dc; pattern = r\"canidae\", sortby = :date_desc)\nCacheAssets.ls!(dc; filename_pattern = r\"\\.csv$\", detail = :full)\n\n# Redirect output\nCacheAssets.ls!(dc; io = my_io)\n\n# Use default cache\nCacheAssets.ls!()\nCacheAssets.ls!(; detail = :full, sortby = :date_desc)","title":"List assets"},{"category":"section","location":"cache_assets/#Remove-assets","page":"Cache Assets","text":"# Remove by label, sequence index, UUID prefix, or CacheEntry — in any combination\nCacheAssets.rm(dc, \"canidae_occs\")\nCacheAssets.rm(dc, 3)\nCacheAssets.rm(dc, \"canidae_occs\", \"dinosaur_taxa\", 5)\n\n# Pass a vector of specifiers (any mix of CacheEntry, label String, seq Integer)\nstale = CacheAssets.ls(dc; before = DateTime(\"2026-01-01\"))\nCacheAssets.rm(dc, stale)                              # remove all at once (one index rewrite)\nCacheAssets.rm(dc, [entry1, \"old_label\", 7])           # mixed-type vector\n\n# Suppress errors for missing specifiers\nCacheAssets.rm(dc, \"maybe_exists\"; force = true)\nCacheAssets.rm(dc, [\"maybe1\", \"maybe2\"]; force = true)\n\n# Default cache\nCacheAssets.rm(\"old_entry\")\nCacheAssets.rm(stale)","title":"Remove assets"},{"category":"section","location":"cache_assets/#Relabel-and-move-assets","page":"Cache Assets","text":"# Relabel within the same cache (dest is a String)\nCacheAssets.mv(dc, \"old_label\", \"new_label\")\nCacheAssets.mv(dc, 2, \"new_label\")             # by sequence index\n\n# Relabel with conflict resolution\nCacheAssets.mv(dc, \"old\", \"taken_label\"; force = true)  # replaces the conflicting entry\n\n# Move to another cache (dest is a DataCache)\ndc2 = DataCache(:archive)\nCacheAssets.mv(dc, \"canidae_occs\", dc2)\nCacheAssets.mv(dc, \"canidae_occs\", dc2; label = \"renamed_in_dest\")\nCacheAssets.mv(dc, \"canidae_occs\", dc2; force = true)   # overwrite if label exists in dc2\n\n# Default-cache forms\nCacheAssets.mv(\"old_label\", \"new_label\")\nCacheAssets.mv(\"entry\", dc2)","title":"Relabel and move assets"},{"category":"section","location":"cache_assets/#Copy-assets-to-another-cache","page":"Cache Assets","text":"# Single asset\nCacheAssets.cp(dc, \"canidae_occs\", dc2)\nCacheAssets.cp(dc, \"canidae_occs\", dc2; label = \"canidae_copy\")\n\n# Multiple assets (vector of specifiers)\nCacheAssets.cp(dc, [\"canidae_occs\", \"dinosaur_taxa\", 5], dc2)\nCacheAssets.cp(dc, [\"canidae_occs\", \"dinosaur_taxa\"], dc2; force = true)\n\n# Default-cache form\nCacheAssets.cp(\"canidae_occs\", dc2)\nCacheAssets.cp([\"entry1\", \"entry2\"], dc2)\n\nEach copy receives a new UUID, sequence number, and datecached timestamp in the destination. Copying within the same cache is allowed and produces a distinct new entry.","title":"Copy assets to another cache"},{"category":"section","location":"cache_assets/#Access-time-tracking-and-LRU-support","page":"Cache Assets","text":"Every DataCache records when each asset was last read via the dateaccessed field on each CacheEntry. This is enabled by default; disable it for caches that are read at very high frequency or have many entries, since every read triggers a full index rewrite:\n\ndc = DataCache(:large_cache; track_access = false)\n\nCacheAssets.ls(dc; detail = :full) shows both datecached and dateaccessed for each entry. Sort by :dateaccessed or :dateaccessed_desc to find least-recently-used assets.","title":"Access-time tracking and LRU support"},{"category":"section","location":"cache_assets/#Quick-reference-table","page":"Cache Assets","text":"Function Description\nCacheAssets.ls([dc]; pattern, filepath_pattern, filename_pattern, format, before, after, accessed_before_date, accessed_after_date, labeled, sortby, rev) List assets in a cache\nCacheAssets.ls!([dc]; detail, …same filters…, io) Print formatted listing to io\nCacheAssets.rm([dc,] assets...; force) Remove assets by varargs (batched index rewrite)\nCacheAssets.rm([dc,] specs::Vector; force) Remove assets by vector (batched index rewrite)\ndelete!(dc, specs::Vector) Remove assets by vector from DataCache directly\nCacheAssets.mv([dc,] src, new_label; force) Relabel an asset within a cache\nCacheAssets.mv([src_dc,] src, dest_dc; label, force) Move asset to another cache\nCacheAssets.cp([src_dc,] src, dest_dc; label, force) Copy asset to another cache\nCacheAssets.cp([src_dc,] srcs::Vector, dest_dc; force) Copy multiple assets to another cache\nCacheAssets.purge!([dc]; max_age, max_idle, keep_count, max_size_bytes, stale, keep_labeled, dry_run, …ls filters…) Bulk-delete entries by age, idle time, count, or size (LRU)","title":"Quick-reference table"},{"category":"section","location":"cache_assets/#API-reference","page":"Cache Assets","text":"","title":"API reference"},{"category":"method","location":"cache_assets/#DataCaches.CacheAssets.cp-Tuple{DataCache, Any, DataCache}","page":"Cache Assets","text":"DataCaches.CacheAssets.cp([src_cache::DataCache,] src, dest_cache::DataCache; kwargs...)\nDataCaches.CacheAssets.cp([src_cache::DataCache,] srcs::AbstractVector, dest_cache::DataCache; kwargs...)\n\nCopy one or more assets to dest_cache. Each copy receives a new UUID, sequence number, and datecached timestamp in the destination. Copying within the same cache is allowed and produces a distinct new entry.\n\nsrc / each element of srcs can be a CacheEntry, label String, UUID-prefix String, or sequence index Integer.\n\nSingle-source form:\n\nlabel=\"\" — override the destination label (default: preserve original label)\nforce=false — when true, silently replace an existing entry with the same label\n\nMulti-source form (srcs::AbstractVector):\n\nEach entry preserves its original label; returns a Vector{CacheKey} of new keys\nforce=false — apply to all entries\n\nIf src_cache is omitted, default_filecache() is used as the source.\n\n\n\n\n\n","title":"DataCaches.CacheAssets.cp"},{"category":"method","location":"cache_assets/#DataCaches.CacheAssets.ls!-Tuple{DataCache}","page":"Cache Assets","text":"DataCaches.CacheAssets.ls!([cache::DataCache]; kwargs...) → nothing\n\nPrint a formatted listing of assets in cache to io (default stdout). If cache is omitted, the active default cache is used (see default_filecache()).\n\nAccepts all the same filtering and sorting keyword arguments as ls, plus:\n\nAdditional keyword arguments\n\nDisplay:\n\ndetail::Symbol = :normal — richness of the output:\n:minimal — sequence index and label only\n:normal (default) — index, write timestamp, UUID prefix, label, and file path\n:full — all of :normal plus last-access time, file size, and data format\nio::IO = stdout\n\n\n\n\n\n","title":"DataCaches.CacheAssets.ls!"},{"category":"method","location":"cache_assets/#DataCaches.CacheAssets.ls-Tuple{DataCache}","page":"Cache Assets","text":"DataCaches.CacheAssets.ls([cache::DataCache]; kwargs...) → Vector{CacheEntry}\n\nReturn a filtered and sorted vector of cache entries from cache. If cache is omitted, the active default cache is used (see default_filecache()).\n\nnote: Note\nDataCaches.entries exposes the same functionality at the top level without needing to import CacheAssets.\n\nKeyword arguments\n\nFiltering:\n\npattern — Regex or String (converted to Regex); matched against label, description, then UUID. Pass nothing (default) to disable.\nfilepath_pattern — Regex or String; matched against the full absolute file path of each entry's backing data file.\nfilename_pattern — Regex or String; matched against only the filename (i.e. basename(path)) of each entry's backing data file.\nbefore::DateTime — include only entries written before this time\nafter::DateTime — include only entries written after this time\naccessed_before_date::DateTime — include only entries last accessed before this time\naccessed_after_date::DateTime — include only entries last accessed after this time\nlabeled::Union{Nothing,Bool} = nothing — true for labeled entries only, false for unlabeled only, nothing for all\nmissing_file::Bool = false — when true, include entries whose backing file is absent\nformat::Union{Nothing,String,Regex} = nothing — String for exact format tag match (e.g. \"csv\", \"jls\"), Regex for pattern match (e.g. r\"csv|json\")\n\nSorting:\n\nsortby::Symbol = :seq — sort criterion: :seq, :label, :date, :date_desc, :dateaccessed, :dateaccessed_desc, :size, :size_desc\nrev::Bool = false — reverse the sort order\n\nSee also ls! for a display-oriented variant that prints to an IO stream.\n\n\n\n\n\n","title":"DataCaches.CacheAssets.ls"},{"category":"method","location":"cache_assets/#DataCaches.CacheAssets.mv-Tuple{DataCache, Any, AbstractString}","page":"Cache Assets","text":"DataCaches.CacheAssets.mv([src_cache::DataCache,] src, dest; kwargs...)\n\nMove or relabel a cache asset. src can be a CacheEntry, label String, UUID-prefix String, or sequence index Integer.\n\nRelabel within the same cache (when dest is a String):\n\nmv(cache, src, new_label::String; force=false)\n\nRename src to new_label in place. If new_label is already taken by a different entry, an error is raised unless force=true, in which case the conflicting entry is removed first.\n\nMove to another cache (when dest is a DataCache):\n\nmv(src_cache, src, dest_cache::DataCache; label=\"\", force=false)\n\nRead the asset from src_cache, write it to dest_cache (with a new UUID, sequence number, and datecached timestamp), then remove it from src_cache. The label kwarg overrides the destination label (default: preserve the original). Pass force=true to overwrite an existing entry with the same label in dest_cache.\n\nIf src_cache is omitted, default_filecache() is used as the source.\n\n\n\n\n\n","title":"DataCaches.CacheAssets.mv"},{"category":"method","location":"cache_assets/#DataCaches.CacheAssets.purge!-Tuple{DataCache}","page":"Cache Assets","text":"DataCaches.CacheAssets.purge!([cache::DataCache]; kwargs...) → DataCache\n\nBulk-delete cache entries matching the given criteria. All standard ls filtering keyword arguments are accepted to scope which entries are candidates for purging. When cache is omitted, the active default cache is used (see default_filecache()).\n\nAll removals are batched into a single index rewrite.\n\nStandard filter kwargs (same as ls)\n\npattern, filepath_pattern, filename_pattern — label/path/filename pattern\nformat::Union{Nothing,String,Regex} — format tag filter (e.g. \"jls\", r\"csv|json\")\nbefore, after — filter by datecached\naccessed_before_date, accessed_after_date — filter by dateaccessed\nlabeled::Union{Nothing,Bool} — true = labeled only; false = unlabeled only\n\nPurge criteria (applied after filtering)\n\nstale::Bool = false — delete entries past their TTL (requires TTL to be configured)\nmax_age::Union{Nothing,Period} — delete entries whose age (by datecached) exceeds this\nmax_idle::Union{Nothing,Period} — delete entries that have not been accessed within this period (entries never accessed are treated as least-recently-used and are eligible)\nkeep_count::Union{Nothing,Int} — keep only the N most-recently-accessed entries among the candidates; delete the rest\nmax_size_bytes::Union{Nothing,Int} — purge LRU entries until the total size of the remaining candidates is at or below this limit\n\nOptions\n\nkeep_labeled::Bool = false — when true, labeled entries are never deleted, even if they match all other criteria\ndry_run::Bool = false — print what would be deleted to io without deleting\nio::IO = stdout — output stream for dry_run messages\n\nExamples\n\ndc = DataCache(:myproject)\n\n# Remove all stale entries (requires TTL on entries or cache)\nCacheAssets.purge!(dc; stale = true)\n\n# Remove entries older than 30 days\nCacheAssets.purge!(dc; max_age = Dates.Day(30))\n\n# Keep only the 10 most recently accessed entries\nCacheAssets.purge!(dc; keep_count = 10)\n\n# Purge LRU entries until cache is under 100 MiB, keep labeled entries\nCacheAssets.purge!(dc; max_size_bytes = 100 * 1024 * 1024, keep_labeled = true)\n\n# Preview without deleting\nCacheAssets.purge!(dc; max_age = Dates.Day(7), dry_run = true)\n\n# Purge using default cache\nCacheAssets.purge!(; keep_count = 50)\n\nSee also ls, rm, DataCaches.invalidate!, DataCaches.set_autopurge!.\n\n\n\n\n\n","title":"DataCaches.CacheAssets.purge!"},{"category":"method","location":"cache_assets/#DataCaches.CacheAssets.rm-Tuple{DataCache, Vararg{Any}}","page":"Cache Assets","text":"DataCaches.CacheAssets.rm([cache::DataCache,] assets...; force=false)\nDataCaches.CacheAssets.rm([cache::DataCache,] specs::AbstractVector; force=false)\n\nRemove one or more assets from cache (default: active default cache). Each asset can be specified as a CacheEntry, label String, UUID-prefix String, or sequence index Integer. The backing data file is also deleted from disk.\n\nAll removals are batched into a single index rewrite for efficiency.\n\nThe AbstractVector form accepts a vector of any mix of the above specifier types, which is convenient for operating on the result of ls.\n\nPass force=true to suppress errors for unresolvable specifiers (they are silently skipped).\n\n\n\n\n\n","title":"DataCaches.CacheAssets.rm"},{"category":"section","location":"caches/#Caches-—-store-management","page":"Caches","text":"DataCaches.Caches is a submodule (public, not exported) that provides a filesystem-style interface for managing the caches living in the DataCaches scratchspace directory (~/.julia/scratchspaces/<DataCaches-UUID>/).\n\nThe scratchspace organises stores into subdirectories by kind:\n\n~/.julia/scratchspaces/<DataCaches-UUID>/\n  caches/\n    user/\n      _DEFAULT/             ← DataCache() / DataCache(:_DEFAULT) default store\n      <name>/              ← DataCache(:name) stores\n    module/<uuid>/<key>/   ← scratch_datacache!(uuid, key) stores\n\nThe Caches submodule lets you inspect, rename, copy, move, and remove stores without needing to construct or track the underlying path yourself.\n\nAccess all functions as DataCaches.Caches.<function>.","title":"Caches — store management"},{"category":"section","location":"caches/#Inspection","page":"Caches","text":"using DataCaches\n\n# Scratchspace root — the top-level scratchspaces directory for DataCaches\nDataCaches.Caches.pwd()\n# → \"/home/user/.julia/scratchspaces/c1455f2b-6d6f-4f37-b463-919f923708a5\"\n\n# Path to a specific user store (directory need not exist yet)\nDataCaches.Caches.pwd(:myproject)\n# → \".../c1455f2b-.../caches/user/myproject\"\n\n# Path to the default store (respects DATACACHES_DEFAULT_STORE env var)\nDataCaches.Caches.defaultstore()\n# → \".../c1455f2b-.../caches/user/_DEFAULT\"\n\n# Listing of the caches directory root (default)\nDataCaches.Caches.ls()\n# → [:user, :module]\n\n# List user stores (DataCache(:name))\nDataCaches.Caches.ls(:user)\n# → [:_DEFAULT, :myproject, :taxonomy, :archived_results]\n\n# List module-scoped stores (scratch_datacache!(uuid, key))\nDataCaches.Caches.ls(:module)\n# → [Symbol(\"00000000-.../results\"), Symbol(\"aaaabbbb-.../datacache\")]","title":"Inspection"},{"category":"section","location":"caches/#Renaming-and-copying-within-the-scratchspace","page":"Caches","text":"# Create some stores\nqueries = DataCache(:pbdb_queries)\ntaxa    = DataCache(:taxonomy)\n\n# Rename\nDataCaches.Caches.mv(:pbdb_queries, :paleodb_queries)\n\n# Duplicate\nDataCaches.Caches.cp(:taxonomy, :taxonomy_backup)\n\n# Remove\nDataCaches.Caches.rm(:taxonomy_backup)\nDataCaches.Caches.rm(:nonexistent; force=true)  # no-op if absent","title":"Renaming and copying within the scratchspace"},{"category":"section","location":"caches/#Moving-and-copying-between-scratchspace-and-filesystem","page":"Caches","text":"The mv and cp functions accept one Symbol (store name) and one AbstractString (filesystem path) in either order:\n\n# Export — move a named cache out of the scratchspace to a filesystem path\nDataCaches.Caches.mv(:paleodb_queries, \"/data/exports/paleodb_queries\")\n\n# Import — move a filesystem directory into the scratchspace as a named cache\nDataCaches.Caches.mv(\"/data/imports/shared_cache\", :shared)\n\n# Export copy (source stays in scratchspace)\nDataCaches.Caches.cp(:taxonomy, \"/tmp/taxonomy_snapshot\")\n\n# Import copy (source stays on filesystem)\nDataCaches.Caches.cp(\"/data/reference/baseline\", :baseline)\n\nThese round-trip cleanly with importcache! and movecache!: use Caches.mv/Caches.cp when working entirely with named stores, and importcache!/movecache! when working with arbitrary DataCache objects.","title":"Moving and copying between scratchspace and filesystem"},{"category":"section","location":"caches/#Quick-reference-table","page":"Caches","text":"Function Description\nCaches.pwd() Scratchspace root path\nCaches.pwd(:name) Path to a user named store (caches/user/<name>)\nCaches.defaultstore() Path to the default store (caches/user/_DEFAULT)\nCaches.ls() Returns Vector{Symbol} — caches root listing (same as ls(:root))\nCaches.ls(:root) Returns Vector{Symbol} — top-level caches subdirs (e.g. [:user, :module])\nCaches.ls(:user) Returns Vector{Symbol} — names of DataCache(:name) stores\nCaches.ls(:module) Returns Vector{Symbol} — \"<uuid>/<key>\" entries for scratch_datacache! stores\nCaches.ls!(storetype; io) Prints the result of ls(storetype) to io (default stdout), returns nothing\nCaches.rm(:name; force=false) Remove a user named store\nCaches.mv(:old, :new) Rename user store within scratchspace\nCaches.mv(:name, path) Move (export) user store to filesystem path\nCaches.mv(path, :name) Move (import) filesystem directory into scratchspace\nCaches.cp(:old, :new) Copy user store within scratchspace\nCaches.cp(:name, path) Copy (export) user store to filesystem path\nCaches.cp(path, :name) Copy (import) filesystem directory into scratchspace","title":"Quick-reference table"},{"category":"section","location":"caches/#API-reference","page":"Caches","text":"","title":"API reference"},{"category":"method","location":"caches/#DataCaches.Caches.cp-Tuple{Symbol, Symbol}","page":"Caches","text":"DataCaches.Caches.cp(src::Symbol, dst::Symbol)\nDataCaches.Caches.cp(src::Symbol, dst::AbstractString)\nDataCaches.Caches.cp(src::AbstractString, dst::Symbol)\n\nCopy a cache store. Three dispatch forms:\n\ncp(src::Symbol, dst::Symbol) — copy depot cache src to dst within the user depot stores.\ncp(src::Symbol, dst::AbstractString) — copy named depot cache src to filesystem path dst (export copy from depot).\ncp(src::AbstractString, dst::Symbol) — copy a filesystem directory src into the depot as dst (import copy into depot).\n\nRaises an error if the source does not exist or the destination already exists.\n\n\n\n\n\n","title":"DataCaches.Caches.cp"},{"category":"method","location":"caches/#DataCaches.Caches.defaultstore-Tuple{}","page":"Caches","text":"DataCaches.Caches.defaultstore() → String\n\nReturn the absolute path to the default DataCache store.\n\nRespects the DATACACHES_DEFAULT_STORE environment variable; otherwise returns the path of the _DEFAULT store inside <root>/caches/user/>.\n\nUnlike DataCache(), this function does not create the directory.\n\n\n\n\n\n","title":"DataCaches.Caches.defaultstore"},{"category":"function","location":"caches/#DataCaches.Caches.ls","page":"Caches","text":"DataCaches.Caches.ls(storetype::Symbol = :root) → Vector{Symbol}\n\nList the names of store categories or stores in the DataCaches cache directory.\n\nstoretype controls which category is listed:\n\n:root (default) — subdirectory listing of the caches root directory (e.g. [:user, :module]).\n:user — named stores created via DataCache(:name), living under <caches>/user/. Returns store names (e.g. [:myproject, :mydata]).\n:module — stores created via scratch_datacache!(uuid, key), living under <caches>/module/<uuid>/<key>/. Returns Symbol(\"<uuid>/<key>\") entries.\n\nReturns an empty vector if the relevant directory does not yet exist.\n\nSee also ls! for a display-oriented variant that prints to an IO stream.\n\n\n\n\n\n","title":"DataCaches.Caches.ls"},{"category":"function","location":"caches/#DataCaches.Caches.ls!","page":"Caches","text":"DataCaches.Caches.ls!(storetype::Symbol = :root; io::IO = stdout) → nothing\n\nPrint the names of store categories or stores in the DataCaches cache directory to io.\n\nAccepts the same storetype argument as ls. Returns nothing.\n\n\n\n\n\n","title":"DataCaches.Caches.ls!"},{"category":"method","location":"caches/#DataCaches.Caches.mv-Tuple{Symbol, Symbol}","page":"Caches","text":"DataCaches.Caches.mv(src::Symbol, dst::Symbol)\nDataCaches.Caches.mv(src::Symbol, dst::AbstractString)\nDataCaches.Caches.mv(src::AbstractString, dst::Symbol)\n\nMove a cache store. Three dispatch forms:\n\nmv(src::Symbol, dst::Symbol) — rename src to dst within the user depot stores.\nmv(src::Symbol, dst::AbstractString) — move named depot cache src to filesystem path dst (export from depot).\nmv(src::AbstractString, dst::Symbol) — move a filesystem directory src into the depot as dst (import into depot).\n\nRaises an error if the source does not exist or the destination already exists.\n\n\n\n\n\n","title":"DataCaches.Caches.mv"},{"category":"method","location":"caches/#DataCaches.Caches.pwd-Tuple{}","page":"Caches","text":"DataCaches.Caches.pwd() → String\nDataCaches.Caches.pwd(name::Symbol) → String\n\nReturn the absolute path to the DataCaches scratchspace root (no argument), or the path to the named user store name within the scratchspace (one argument).\n\nThe root is the scratchspaces directory managed by Scratch.jl for DataCaches. User stores live under <root>/caches/user/ and are created via DataCache(:name).\n\nThe path is returned whether or not the directory currently exists.\n\nExamples\n\nDataCaches.Caches.pwd()          # \"~/.julia/scratchspaces/c1455f2b-.../\nDataCaches.Caches.pwd(:mydata)   # \"~/.julia/scratchspaces/c1455f2b-.../caches/user/mydata\"\n\n\n\n\n\n","title":"DataCaches.Caches.pwd"},{"category":"method","location":"caches/#DataCaches.Caches.rm-Tuple{Symbol}","page":"Caches","text":"DataCaches.Caches.rm(name::Symbol; force=false)\n\nRemove the named local cache store from the depot.\n\nRaises an error if the store does not exist, unless force=true (in which case a missing store is silently ignored).\n\n\n\n\n\n","title":"DataCaches.Caches.rm"},{"category":"section","location":"expiration/#Cache-expiration-and-invalidation","page":"Expiration & Invalidation","text":"DataCaches provides first-class support for cache invalidation: time-to-live (TTL), stale detection, bulk invalidation, fine-grained purging, and automatic post-write purge policies.","title":"Cache expiration and invalidation"},{"category":"section","location":"expiration/#TTL-and-staleness","page":"Expiration & Invalidation","text":"","title":"TTL and staleness"},{"category":"section","location":"expiration/#Per-entry-TTL","page":"Expiration & Invalidation","text":"Pass a ttl keyword to write! to record a time-to-live for an entry. The value must be a Dates.Period (e.g. Dates.Hour(6), Dates.Day(30)).\n\nusing DataCaches, Dates\n\ndc = DataCache(:myproject)\n\n# Cache a result with a 6-hour TTL\nwrite!(dc, result; label = \"query1\", ttl = Hour(6))\n\n# Check staleness before re-using\nif isstale(dc, \"query1\")\n    result = fetch_live()\n    write!(dc, result; label = \"query1\", ttl = Hour(6))\nend\ndata = read(dc, \"query1\")\n\nread does not enforce staleness — stale entries are readable until explicitly removed. This enables a stale-while-revalidate pattern: serve from the cache while refreshing in the background.","title":"Per-entry TTL"},{"category":"section","location":"expiration/#Cache-level-default-TTL","page":"Expiration & Invalidation","text":"Set a default TTL on the cache itself so every write! inherits it automatically:\n\n# All writes to this cache expire after 24 hours unless overridden per-entry\ndc = DataCache(:myproject; default_ttl = Day(1))\n\nwrite!(dc, result; label = \"query1\")          # inherits Day(1)\nwrite!(dc, result; label = \"query2\", ttl = Hour(1))  # overrides to Hour(1)\n\nThe default_ttl is persisted in the cache's TOML index, so it is remembered when the cache is reloaded in a future session.","title":"Cache-level default TTL"},{"category":"section","location":"expiration/#isstale","page":"Expiration & Invalidation","text":"isstale checks whether an entry has exceeded its TTL:\n\nisstale(dc, \"query1\")           # by label\nisstale(dc, entry)              # by CacheEntry\nisstale(\"query1\")               # uses default_filecache()\nisstale(entry)                  # uses default_filecache()\n\nReturns false when no TTL applies (neither per-entry nor cache default). Legacy entries whose datecached is unknown are never considered stale.","title":"isstale"},{"category":"section","location":"expiration/#Stale-while-revalidate-pattern","page":"Expiration & Invalidation","text":"function get_data(dc, label, fetch_fn; ttl = Hour(6))\n    # Return cached value even if stale; trigger background refresh when stale\n    if haskey(dc, label) && isstale(dc, label)\n        @async begin\n            result = fetch_fn()\n            write!(dc, result; label = label, ttl = ttl)\n        end\n    elseif !haskey(dc, label)\n        result = fetch_fn()\n        write!(dc, result; label = label, ttl = ttl)\n    end\n    return read(dc, label)\nend","title":"Stale-while-revalidate pattern"},{"category":"section","location":"expiration/#invalidate!-—-bulk-invalidation","page":"Expiration & Invalidation","text":"invalidate! removes entries matching a set of criteria in a single batched index rewrite. When called without a cache argument, uses default_filecache().","title":"invalidate! — bulk invalidation"},{"category":"section","location":"expiration/#By-staleness","page":"Expiration & Invalidation","text":"# Remove all entries that have exceeded their TTL\ninvalidate!(dc; stale = true)\ninvalidate!(; stale = true)    # default cache","title":"By staleness"},{"category":"section","location":"expiration/#By-label-pattern","page":"Expiration & Invalidation","text":"# Remove entries whose label/description matches a pattern\ninvalidate!(dc; pattern = r\"^temp_\")\ninvalidate!(dc; pattern = \"old_run_\")          # String is converted to Regex","title":"By label pattern"},{"category":"section","location":"expiration/#By-date","page":"Expiration & Invalidation","text":"# Remove entries cached more than 30 days ago\ninvalidate!(dc; before = now() - Day(30))\n\n# Remove entries from a specific window\ninvalidate!(dc; after  = DateTime(\"2026-01-01T00:00:00\"),\n                before = DateTime(\"2026-02-01T00:00:00\"))","title":"By date"},{"category":"section","location":"expiration/#By-file-format","page":"Expiration & Invalidation","text":"# Remove all JLS (binary serialization) entries\ninvalidate!(dc; format = \"jls\")\ninvalidate!(dc; format = r\"jls|json\")   # multiple formats via Regex","title":"By file format"},{"category":"section","location":"expiration/#By-predicate-function","page":"Expiration & Invalidation","text":"A predicate function receives a CacheEntry and returns true if the entry should be removed:\n\n# Remove entries whose description starts with a particular function name\ninvalidate!(dc; predicate = e -> startswith(e.description, \"my_func(\"))\n\n# Remove large files\ninvalidate!(dc; predicate = e -> isfile(e.path) && stat(e.path).size > 50_000_000)","title":"By predicate function"},{"category":"section","location":"expiration/#Combining-criteria","page":"Expiration & Invalidation","text":"All filter kwargs (same as entries / CacheAssets.ls) are accepted first to scope the candidate set, then stale and predicate are applied as additional restrictions:\n\n# Remove stale, labeled-only entries cached before a date\ninvalidate!(dc; labeled = true, before = now() - Day(7), stale = true)","title":"Combining criteria"},{"category":"section","location":"expiration/#Dry-run-preview","page":"Expiration & Invalidation","text":"invalidate!(dc; stale = true, dry_run = true)\n# Prints: \"invalidate! (dry run): N entries would be removed: …\"\n# No entries are deleted.","title":"Dry-run preview"},{"category":"section","location":"expiration/#CacheAssets.purge!-—-power-tool","page":"Expiration & Invalidation","text":"DataCaches.CacheAssets.purge! is the full-featured purge function with LRU eviction, size limits, and all the filter kwargs accepted by CacheAssets.ls.","title":"CacheAssets.purge! — power tool"},{"category":"section","location":"expiration/#Remove-by-age","page":"Expiration & Invalidation","text":"using DataCaches.CacheAssets\n\n# Remove entries older than 30 days\npurge!(dc; max_age = Day(30))","title":"Remove by age"},{"category":"section","location":"expiration/#Remove-by-idle-time-(LRU)","page":"Expiration & Invalidation","text":"# Remove entries not accessed in the last 7 days\npurge!(dc; max_idle = Day(7))\n\nnote: Note\nmax_idle requires track_access = true (the default) on the cache. Entries that have never been accessed are treated as most-idle.","title":"Remove by idle time (LRU)"},{"category":"section","location":"expiration/#Keep-only-the-N-most-recently-accessed","page":"Expiration & Invalidation","text":"# Keep the 20 most recently accessed entries; delete the rest\npurge!(dc; keep_count = 20)","title":"Keep only the N most recently accessed"},{"category":"section","location":"expiration/#Limit-total-cache-size","page":"Expiration & Invalidation","text":"# Purge LRU entries until total cache size is under 500 MiB\npurge!(dc; max_size_bytes = 500 * 1024 * 1024)","title":"Limit total cache size"},{"category":"section","location":"expiration/#Protect-labeled-entries","page":"Expiration & Invalidation","text":"# Purge only unlabeled entries older than a week; leave labeled entries alone\npurge!(dc; max_age = Day(7), keep_labeled = true)","title":"Protect labeled entries"},{"category":"section","location":"expiration/#Combine-with-format-or-pattern-filters","page":"Expiration & Invalidation","text":"# Delete JLS entries not accessed in the last 30 days\npurge!(dc; format = \"jls\", max_idle = Day(30))\n\n# Delete entries matching a label pattern that are older than a month\npurge!(dc; pattern = r\"^temp_\", max_age = Day(30))","title":"Combine with format or pattern filters"},{"category":"section","location":"expiration/#Dry-run","page":"Expiration & Invalidation","text":"purge!(dc; max_age = Day(7), dry_run = true)\n# Prints: \"purge! (dry run): N entries would be removed: …\"\n# No entries are deleted.","title":"Dry-run"},{"category":"section","location":"expiration/#Remove-all-stale-entries-via-purge!","page":"Expiration & Invalidation","text":"purge!(dc; stale = true)","title":"Remove all stale entries via purge!"},{"category":"section","location":"expiration/#Auto-purge-policies","page":"Expiration & Invalidation","text":"set_autopurge! attaches a PurgePolicy to a cache. The policy runs automatically at the end of every write!, keeping the cache within configured limits without manual intervention.","title":"Auto-purge policies"},{"category":"section","location":"expiration/#LRU-eviction","page":"Expiration & Invalidation","text":"# Keep only the 50 most recently accessed entries\nset_autopurge!(dc; keep_count = 50)\n\n# Write a new result — oldest entries are pruned automatically\nwrite!(dc, new_result; label = \"latest\")","title":"LRU eviction"},{"category":"section","location":"expiration/#Age-based-cleanup","page":"Expiration & Invalidation","text":"# Entries older than 14 days are automatically removed on each write\nset_autopurge!(dc; max_age = Day(14))","title":"Age-based cleanup"},{"category":"section","location":"expiration/#Size-based-cleanup","page":"Expiration & Invalidation","text":"# Cache stays under 1 GiB; LRU entries are pruned when the limit is exceeded\nset_autopurge!(dc; max_size_bytes = 1024^3)","title":"Size-based cleanup"},{"category":"section","location":"expiration/#Combined-policy","page":"Expiration & Invalidation","text":"# Remove entries older than 30 days OR keep only the last 100,\n# but never delete labeled entries\nset_autopurge!(dc; max_age = Day(30), keep_count = 100, keep_labeled = true)","title":"Combined policy"},{"category":"section","location":"expiration/#Disabling-auto-purge","page":"Expiration & Invalidation","text":"set_autopurge!(dc; enabled = false)","title":"Disabling auto-purge"},{"category":"section","location":"expiration/#Using-the-default-cache","page":"Expiration & Invalidation","text":"All functions accept a zero-argument form that operates on default_filecache():\n\nset_autopurge!(; keep_count = 20)\ninvalidate!(; stale = true)\nCacheAssets.purge!(; max_age = Day(30))","title":"Using the default cache"},{"category":"section","location":"expiration/#Quick-reference-table","page":"Expiration & Invalidation","text":"Function Description\nwrite!(dc, data; ttl = Hour(6)) Write with per-entry TTL\nDataCache(store; default_ttl = Day(1)) Cache-level default TTL\nisstale(dc, label) / isstale(entry) Check if entry is past TTL\ninvalidate!(dc; stale, pattern, format, before, after, labeled, predicate, dry_run) Bulk removal by criteria\nCacheAssets.purge!(dc; max_age, max_idle, keep_count, max_size_bytes, stale, keep_labeled, dry_run, …ls filters…) Power tool with LRU/size limits\nset_autopurge!(dc; max_age, max_idle, keep_count, max_size_bytes, keep_labeled, enabled) Configure automatic post-write purging","title":"Quick-reference table"},{"category":"section","location":"expiration/#API-reference","page":"Expiration & Invalidation","text":"","title":"API reference"},{"category":"function","location":"expiration/#DataCaches.isstale","page":"Expiration & Invalidation","text":"isstale(cache::DataCache, entry::CacheEntry) → Bool\nisstale(cache::DataCache, label::AbstractString) → Bool\nisstale(entry::CacheEntry) → Bool\nisstale(label::AbstractString) → Bool\n\nReturn true if entry (or the entry identified by label) has exceeded its time-to-live and is considered stale.\n\nThe effective TTL for an entry is its own entry.ttl if set, otherwise the cache's default_ttl. When no TTL applies (both are nothing), returns false.\n\nStaleness is purely informational — read does not enforce it. The caller decides whether to refresh (enabling stale-while-revalidate patterns).\n\nExamples\n\ndc = DataCache(:myproject; default_ttl = Dates.Hour(1))\nwrite!(dc, result; label = \"query1\")\n\nisstale(dc, \"query1\")   # false immediately after write\n# … one hour later …\nisstale(dc, \"query1\")   # true\n\n# Per-entry TTL overrides the cache default:\nwrite!(dc, result2; label = \"short\", ttl = Dates.Minute(5))\nisstale(dc, \"short\")    # true after 5 minutes\n\nSee also: invalidate!, DataCaches.CacheAssets.purge!.\n\n\n\n\n\n","title":"DataCaches.isstale"},{"category":"function","location":"expiration/#DataCaches.invalidate!","page":"Expiration & Invalidation","text":"invalidate!(cache::DataCache; kwargs...) → DataCache\ninvalidate!(; kwargs...) → DataCache\n\nRemove entries from cache that match the given criteria. All removals are batched into a single index rewrite.\n\nWhen called without a cache argument, uses default_filecache().\n\nFiltering keyword arguments\n\nEntries are first selected by the standard filter set (same as entries):\n\npattern — Regex or String; matched against label, description, or UUID\nfilepath_pattern — Regex or String; matched against full file path\nfilename_pattern — Regex or String; matched against filename only\nformat — String or Regex; matched against the entry's format tag (\"csv\", \"jls\", …)\nbefore::DateTime — entries cached before this time\nafter::DateTime — entries cached after this time\naccessed_before_date::DateTime — entries last accessed before this time\naccessed_after_date::DateTime — entries last accessed after this time\nlabeled::Union{Nothing,Bool} — true = labeled only; false = unlabeled only\n\nInvalidation criteria\n\nstale::Bool = false — further restrict to entries that are past their TTL\npredicate::Union{Nothing,Function} = nothing — further restrict to entries where predicate(entry) returns true\n\nOptions\n\ndry_run::Bool = false — print matching entries to io without deleting\nio::IO = stdout — output stream for dry_run messages\n\nExamples\n\ndc = DataCache(:myproject; default_ttl = Dates.Hour(24))\n\n# Remove all stale entries\ninvalidate!(dc; stale = true)\n\n# Remove entries matching a label pattern\ninvalidate!(dc; pattern = r\"^temp_\")\n\n# Remove CSV entries older than a week\ninvalidate!(dc; format = \"csv\", before = Dates.now() - Dates.Day(7))\n\n# Remove entries matching a custom predicate\ninvalidate!(dc; predicate = e -> startswith(e.description, \"my_func(\"))\n\n# Preview what would be removed\ninvalidate!(dc; stale = true, dry_run = true)\n\nSee also: isstale, DataCaches.CacheAssets.purge!.\n\n\n\n\n\n","title":"DataCaches.invalidate!"},{"category":"function","location":"expiration/#DataCaches.set_autopurge!","page":"Expiration & Invalidation","text":"set_autopurge!(cache::DataCache; enabled=true, kwargs...) → DataCache\nset_autopurge!(; enabled=true, kwargs...) → DataCache\n\nConfigure an automatic purge policy on cache. After each write!, the policy is applied to keep the cache within the specified limits.\n\nWhen called without a cache argument, uses default_filecache().\n\nPass enabled = false to remove the current policy (disabling auto-purge).\n\nKeyword arguments\n\nmax_age::Union{Nothing,Period} — remove entries older than this age (by datecached)\nmax_idle::Union{Nothing,Period} — remove entries not accessed within this period\nkeep_count::Union{Nothing,Int} — keep only the N most-recently-accessed entries\nmax_size_bytes::Union{Nothing,Int} — purge LRU entries until total cache size ≤ limit\nkeep_labeled::Bool = false — when true, labeled entries are never auto-purged\n\nExamples\n\ndc = DataCache(:myproject)\n\n# Keep only the 20 most recently accessed entries\nset_autopurge!(dc; keep_count = 20)\n\n# Remove entries older than 30 days, never delete labeled entries\nset_autopurge!(dc; max_age = Dates.Day(30), keep_labeled = true)\n\n# Disable auto-purge\nset_autopurge!(dc; enabled = false)\n\nSee also: DataCaches.CacheAssets.purge!, invalidate!.\n\n\n\n\n\n","title":"DataCaches.set_autopurge!"},{"category":"function","location":"expiration/#DataCaches.CacheAssets.purge!","page":"Expiration & Invalidation","text":"DataCaches.CacheAssets.purge!([cache::DataCache]; kwargs...) → DataCache\n\nBulk-delete cache entries matching the given criteria. All standard ls filtering keyword arguments are accepted to scope which entries are candidates for purging. When cache is omitted, the active default cache is used (see default_filecache()).\n\nAll removals are batched into a single index rewrite.\n\nStandard filter kwargs (same as ls)\n\npattern, filepath_pattern, filename_pattern — label/path/filename pattern\nformat::Union{Nothing,String,Regex} — format tag filter (e.g. \"jls\", r\"csv|json\")\nbefore, after — filter by datecached\naccessed_before_date, accessed_after_date — filter by dateaccessed\nlabeled::Union{Nothing,Bool} — true = labeled only; false = unlabeled only\n\nPurge criteria (applied after filtering)\n\nstale::Bool = false — delete entries past their TTL (requires TTL to be configured)\nmax_age::Union{Nothing,Period} — delete entries whose age (by datecached) exceeds this\nmax_idle::Union{Nothing,Period} — delete entries that have not been accessed within this period (entries never accessed are treated as least-recently-used and are eligible)\nkeep_count::Union{Nothing,Int} — keep only the N most-recently-accessed entries among the candidates; delete the rest\nmax_size_bytes::Union{Nothing,Int} — purge LRU entries until the total size of the remaining candidates is at or below this limit\n\nOptions\n\nkeep_labeled::Bool = false — when true, labeled entries are never deleted, even if they match all other criteria\ndry_run::Bool = false — print what would be deleted to io without deleting\nio::IO = stdout — output stream for dry_run messages\n\nExamples\n\ndc = DataCache(:myproject)\n\n# Remove all stale entries (requires TTL on entries or cache)\nCacheAssets.purge!(dc; stale = true)\n\n# Remove entries older than 30 days\nCacheAssets.purge!(dc; max_age = Dates.Day(30))\n\n# Keep only the 10 most recently accessed entries\nCacheAssets.purge!(dc; keep_count = 10)\n\n# Purge LRU entries until cache is under 100 MiB, keep labeled entries\nCacheAssets.purge!(dc; max_size_bytes = 100 * 1024 * 1024, keep_labeled = true)\n\n# Preview without deleting\nCacheAssets.purge!(dc; max_age = Dates.Day(7), dry_run = true)\n\n# Purge using default cache\nCacheAssets.purge!(; keep_count = 50)\n\nSee also ls, rm, DataCaches.invalidate!, DataCaches.set_autopurge!.\n\n\n\n\n\n","title":"DataCaches.CacheAssets.purge!"},{"category":"section","location":"#DataCaches.jl","page":"Home","text":"(Image: CI) (Image: Documentation (stable)) (Image: Documentation (dev))\n\nA lightweight, file-backed key-value cache for Julia workflows that make frequent expensive function calls (remote API queries, long-running computations) and need results available across Julia sessions.\n\nDataCaches selects a transparent, inspectable storage format automatically based on the data type, falling back to Julia's binary serialization for types without a dedicated format:\n\nData type Format File Version-stable?\nDataFrame, Tables.jl-compatible CSV .csv Yes\nNamedTuple JSON .json Yes (JSON-primitive values)\nAnything else Julia serialization .jls No\n\nThe format used is recorded per entry in the cache index so the correct deserializer is always selected on read. Custom serializers can be registered for additional types via register_serializer!.\n\nThree levels of caching are provided, from lightest-weight to most manual:\n\nLevel Mechanism Persistence Works with any function?\nMemoized @filecache Across sessions Yes\nRefresh @filecache! Across sessions Yes\nMemoized @memcache In-session only Yes\nExplicit dc[\"label\"] = result Across sessions Yes\nAutomatic set_autocaching! Across sessions Only if instrumented","title":"DataCaches.jl"},{"category":"section","location":"#Installation","page":"Home","text":"At the Julia REPL, press ] to enter package manager mode:\n\npkg> add DataCaches","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"","title":"Quick Start"},{"category":"section","location":"#Cache-store","page":"Home","text":"First, create a DataCache. The simplest form requires no configuration:\n\ndc = DataCache()                                         # lifecycle-managed default\ndc = DataCache(:myproject)                               # named, no path management\ndc = DataCache(joinpath(homedir(), \".datacaches\", \"p1\")) # explicit path","title":"Cache store"},{"category":"section","location":"#Caching-approaches","page":"Home","text":"using DataCaches\n\ndc = DataCache(:myproject)\n\n# Memoized — works with any function, just wrap the call\nset_default_filecache!(dc)\nresult = @filecache some_expensive_query(; taxon = \"Dinosauria\")  # fetches + stores\nresult = @filecache some_expensive_query(; taxon = \"Dinosauria\")  # from cache\nresult = @filecache! some_expensive_query(; taxon = \"Dinosauria\") # force refresh\n\n# Explicit — dict-style, full control over labels\ndc[\"dinosaurs\"] = some_expensive_query(; taxon = \"Dinosauria\")\ndf = dc[\"dinosaurs\"]\n\n# Automatic — zero call-site changes, but requires instrumented functions\n# (see Pattern 3 in the README for setup)\nset_autocaching!(true; cache = dc)\nresult = some_expensive_query(; taxon = \"Dinosauria\")   # fetches + stores\nresult = some_expensive_query(; taxon = \"Dinosauria\")   # from cache, unchanged call\nset_autocaching!(false)","title":"Caching approaches"},{"category":"section","location":"#Further-reading","page":"Home","text":"API Reference — full DataCaches function and type reference\nCaches — filesystem-style management of cache stores in the scratchspace\nCache Assets — inspect, copy, move, and remove individual cache entries\nLibrary Integration — embedding DataCaches.jl in a package","title":"Further reading"}]
}
